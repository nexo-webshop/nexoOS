# nexoOS
Build 0000.0001
- added cli
- far jump is now correct
- added stack alignment
- direction flag reset
- added an failsafe
- finalized segment setup
- fixed ORG + Memory Layout
- fixed that stage 2 was in the same section as stage 1
- fixed an issue that the stack was unsafe
- fixed that the segment registers were unconsistent
- better memory layout
- forced segment registers
- stack fully initialized
- VGA write is now improved
- added hlt for lower CPU usage
- expanded debug output.

# ROLLING RELEASE!
